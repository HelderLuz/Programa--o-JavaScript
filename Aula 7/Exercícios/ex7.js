// 7. Crie um programa que leia um caractere e informe se ele é uma vogal ou uma consoante.

let char = prompt('Charactere: ')

if (char === 'a' || char === 'e' || char === 'i' || char === 'o' || char === 'u') {
    console.log('vogal')
} else {
    console.log('consoante')
}